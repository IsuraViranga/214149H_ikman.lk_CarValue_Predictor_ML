__pycache__/
*.pyc
*.pyo
*.log
.env
venv/
env/

# Tests never ship in the production image. CI runs them directly on the
# runner, and locally they run via a bind mount (see below).
tests/
requirements-dev.txt
.pytest_cache/
